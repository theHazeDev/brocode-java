package com.variablescopejava;

public class VariableScopeJava {

    public static void main(String[] args) {

        // local = declared within/inside a method visible only to that method
        // global = declared outside a method, but within a class visible to all
        //          parts of a class


        Diceroller diceroller = new Diceroller();


    }
}
